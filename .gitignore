# Personal/local overrides (user-specific, never push)
local/config.json
local/agents/*

# Shipped local/ scaffolding
!local/README.md
!local/config.json.template
!local/agents/.gitkeep

# Legacy per-user SQLite memory DB location from pre-user-local versions
omc-memory.db

# Personal agent files accidentally placed in agents/
*.personal.agent.md

# Personal config
personal-config.json
